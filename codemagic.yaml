workflows:
  ios-build:
    name: iOS Build
    max_build_duration: 70
    environment:
      flutter: stable
      xcode: latest
      cocoapods: default
    scripts:
      - name: Get Flutter dependencies
        script: flutter pub get
      - name: Build iOS app
        script: flutter build ipa
    artifacts:
      - build/ios/ipa/*.ipa


